[tool.mutmut]
# Директории для запуска тестов
tests_dir = "tests/"

# Исходный код, который нужно мутировать
paths_to_mutate = [
    "handlers/",
    "database/",
    "utils.py",
    "bot.py",
]
runner = "pytest -x" # Останавливать тесты после первой ошибки для ускорения